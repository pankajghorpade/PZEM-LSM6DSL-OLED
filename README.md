# PZEM-LSM6DSL-OLED
An embedded application that reads real-time voltage data from the PZEM-004T power meter (Modbus/RTU over UART) and 6-axis IMU data from the LSM6DSL sensor (I²C), and displays output on an SSD1309 OLED screen. Runs on FreeRTOS and uses SPI, UART, and I²C interfaces.
